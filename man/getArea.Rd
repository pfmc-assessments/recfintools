% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getArea.R
\name{getArea}
\alias{getArea}
\title{Filter out records from Puget Sound, Canada, Mexico, or unknown areas based
on input column specified in \code{source}.}
\usage{
getArea(data, source = c("RECFIN_WATER_AREA_NAME"), verbose = TRUE)
}
\arguments{
\item{data}{A loaded Rdata object from pull_catch_recfin_ or
pull_bds_recfin_.}

\item{source}{Column name where area information is located. Depends on the
type of data (catch or bds) and era (recent, mrfss, or historical).
Coded to accept a vector of names where the same type or era of data has
multiple different names. When multiple names within the vector are in the
dataset, picks the first.
For recent catch data, use \code{RECFIN_WATER_AREA_NAME}, which filters out values
of Canada, Mexico, and Puget Sound. Areas with \verb{Not Known} are kept.
For Washington historical catch data, uses \code{AREA}, which filters out values
of 5 and greater (i.e. Puget Sound)
For recent bds data, use \code{AGENCY_FISHED_AREA_NAME}, which filters out values
of Canada, Mexico, and Puget Sound. Areas with "Not known" or "Unknown"
are kept if they also have coastal port names. Because California data are
empty for \code{AGENCY_FISH_AREA_NAME}, the code instead filters California data
using "AGENCY_WATER_AREA_NAME" when \code{AGENCY_FISHED_AREA_NAME} is used.
For all other data sets, use any valid column, since for these areas no
specific records outside federal waters are identifiable.}

\item{verbose}{Whether to output detailed information about the cleaning
process. Default is TRUE.}
}
\description{
Filter out records from Puget Sound, Canada, Mexico, or unknown areas based
on input column specified in \code{source}.
}
\details{
This function is used for both catch and composition data. Because the case
for values are sometimes different across data sets, fields with values as
character strings are converted to lowercase when filtering.
Based on similar function from pacfintools, but modified for recreational data
}
\section{Area filtering rules}{

Values in \code{source} are evaluated using case-insensitive matching.

For recent RecFIN catch data (\code{source = "RECFIN_WATER_AREA_NAME"}), records
are removed when area is:
\itemize{
\item \code{CANADA}
\item \code{MEXICO}
\item \verb{PUGET SOUND}
}

For recent RecFIN bds data (\code{source = "AGENCY_FISHED_AREA_NAME"}), records
are kept when area is
\itemize{
\item For Washington: PUNCH CARD AREAs 1 through 4, and PUNCH CARD AREAs 0 and
'Not Known' when RECFIN_PORT_NAME also equals coastal ports.
\item For California: Because AGENCY_FISHED_AREA_NAME is empty for California,
the script automatically uses "AGENCY_WATER_AREA_NAME" for california data.
}

For Washington historical data (\code{source = "AREA"} and \code{AGENCY == "W"}),
records with \code{AREA >= 5} are removed.

If \code{verbose = TRUE}, the function reports the number of records removed by
category.
}

\seealso{
\code{\link[=clean_catch]{clean_catch()}} calls 'getArea'
}
