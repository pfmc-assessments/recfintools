% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getState.R
\name{getState}
\alias{getState}
\title{Create a state column based on input column specified in \code{source}}
\usage{
getState(data, source = c("AGENCY", "STATE_NAME"), verbose = TRUE)
}
\arguments{
\item{data}{A loaded Rdata object from pull_catch_recfin_ or
pull_bds_recfin_.}

\item{source}{Column name where state information is located. Depends on the
type of data (catch or bds) and era (recent, mrfss, or historical). Default
value is for recent catch data (i.e. AGENCY for CTE001 or STATE_NAME for CTE501).
Coded to accept a vector of names where the same type or era of data has
multiple different names. When multiple names within the vector are in the
dataset, picks the first.}

\item{verbose}{Whether to output detailed information about the cleaning
process. Default is TRUE.}
}
\description{
Create a state column based on input column specified in \code{source}
}
\details{
Copied from pacfintools, and modified for recreational data
This function is used for both catch and composition data
}
\section{State mapping rules}{

\code{source} can be a vector of candidate column names. The first matching
column in \code{data} is used.

Values are standardized into \code{state} as:
\itemize{
\item \code{WA}: \code{WASHINGTON}, \code{W}, or \code{53}
\item \code{OR}: \code{OREGON}, \code{O}, or \code{41}
\item \code{CA}: \code{CALIFORNIA}, \code{C}, or \code{6}
\item \code{UNK}: all unmatched values
}

If \code{verbose = TRUE}, the function reports how many records were assigned
\code{UNK}.
}

\seealso{
\code{\link[=clean_catch]{clean_catch()}} calls 'getState'
}
